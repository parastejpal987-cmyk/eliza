/**
 * Compatibility facade for runtime-owned first-run contracts.
 *
 * Executable normalization and provider-catalog behavior lives in
 * `@elizaos/core`; this path remains stable for browser and host consumers.
 */
export * from "@elizaos/core/contracts/first-run-options";
