/**
 * Compatibility facade for runtime-owned service-routing contracts.
 *
 * Keeping this module as a re-export preserves existing shared-package import
 * paths without maintaining a second executable normalizer implementation.
 */
export * from "@elizaos/core/contracts/service-routing";
