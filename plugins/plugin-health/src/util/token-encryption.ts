/** Compatibility export for core's canonical local connector-token encryption implementation. */
export {
  decryptTokenEnvelope,
  type EncryptedTokenEnvelope,
  encryptTokenPayload,
  isEncryptedTokenEnvelope,
  resolveTokenEncryptionKey,
} from "@elizaos/core";
